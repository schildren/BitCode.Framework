apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-api-config
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  Logging__LogLevel__Default: "Warning"
  # Producción: proveedor de secretos de nivel empresarial (Vault, ADR 0014),
  # nunca el default de desarrollo/CI.
  Secrets__Provider: "Vault"
