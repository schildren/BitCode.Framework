apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-api
spec:
  # F4-06 (HPA) todavía no aplica -- dev corre con réplica fija baja.
  replicas: 1
  template:
    spec:
      containers:
        - name: sample-api
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
